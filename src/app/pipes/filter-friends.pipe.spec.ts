import { FilterFriendsPipe } from './filter-friends.pipe';

describe('FilterFriendsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFriendsPipe();
    expect(pipe).toBeTruthy();
  });
});
